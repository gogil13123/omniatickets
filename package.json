{
    "name": "omniatickets",
    "version": "1.0.0",
    "description": "Omnia Tickets Monorepo",
    "scripts": {
        "start": "node backend/server.js",
        "build": "cd frontend && npm install && npm run build && cd ../backend && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}